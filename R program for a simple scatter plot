library(ggplot2)

# Sample data
data <- data.frame(
  x = c(1, 2, 3, 4, 5),
  y = c(2, 4, 5, 4, 5)
)

# Scatter plot
ggplot(data, aes(x = x, y = y)) +
  geom_point(size = 3, color = "blue") +
  ggtitle("Sample Scatter Plot") +
  theme_minimal()
